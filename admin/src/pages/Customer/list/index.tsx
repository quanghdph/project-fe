import React, { Fragment } from 'react';
import Customers from 'src/components/Customers/list';

const CustomerListPage = () => {
    return (
        <Fragment>
            <Customers />
        </Fragment>
    );
};

export default CustomerListPage;