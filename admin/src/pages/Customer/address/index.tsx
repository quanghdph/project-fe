import React, { Fragment } from 'react';
import AddressPage from 'src/components/Customers/address';

const AddressListPage = () => {
    return (
        <Fragment>
            <AddressPage />
        </Fragment>
    );
};

export default AddressListPage;