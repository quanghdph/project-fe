import React, { Fragment } from 'react';
import Size from 'src/components/Catalog/Size/list';

const SizeListPage = () => {
    return (
        <Fragment>
            <Size />
        </Fragment>
    );
};

export default SizeListPage;