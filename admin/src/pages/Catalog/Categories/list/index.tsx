import React, { Fragment } from 'react';
import Categories from 'src/components/Catalog/Categories/list';

const CategoryListPage = () => {
    return (
        <Fragment>
            <Categories />
        </Fragment>
    );
};

export default CategoryListPage;