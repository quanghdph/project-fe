import React, { Fragment } from 'react';
import Color from 'src/components/Catalog/Color/list';

const ColorListPage = () => {
    return (
        <Fragment>
            <Color />
        </Fragment>
    );
};

export default ColorListPage;