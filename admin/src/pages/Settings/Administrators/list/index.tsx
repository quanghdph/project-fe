import React, { Fragment } from 'react';
import Administrators from 'src/components/Settings/Administrators/list';

const AdministratorListPage = () => {
    return (
        <Fragment>
            <Administrators />
        </Fragment>
    );
};

export default AdministratorListPage;